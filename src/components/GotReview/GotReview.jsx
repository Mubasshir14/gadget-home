import React from 'react';
import useReview from '../hooks/useReview';

const GotReview = () => {
    const [review] = useReview()
    
    return (
        <div>
            
        </div>
    );
};

export default GotReview;